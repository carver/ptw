[package]
name = "ptw-core"
description = "Push to Whisper: the host-independent parts (hotkey chords, custom-word correction, hold-back, engine and typist traits)"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
toml.workspace = true
thiserror.workspace = true
tracing.workspace = true
strsim = "0.11"
rphonetic = "3"

[dev-dependencies]
proptest.workspace = true
